<h4 class='text-center mt-2'>タグ検索</h4>
<!--タグリスト-->
<% tag_list.each do |list| %>
  <i class="fa-sharp fa-solid fa-tag"></i>
  <%= link_to list.name,admin_search_tag_path(tag_id: list.id) %>
  <%= "(#{list.posts.count})" %>
<% end %>